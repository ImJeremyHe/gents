[package]
name = "tests"
version = "0.1.0"
edition = "2024"

[dependencies]
gents = { path = "../" }
gents_derives = { path = "../derives" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"

[features]
default = ["gents"]
gents = []
